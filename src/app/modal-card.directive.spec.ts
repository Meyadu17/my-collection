import { ModalCardDirective } from './modal-card.directive';

describe('ModalCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalCardDirective();
    expect(directive).toBeTruthy();
  });
});
